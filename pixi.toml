[workspace]
name = "prefect-base"
version = "0.1.0"
description = "Base Prefect installation for airgapped environment"
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
prefect = ">=3.4.10,<4"
ansible = ">=12.0.0,<13"

deployments:
  - name: demo_flow
    entrypoint: main.py:my_workflow
    work_pool:
      name: process-pool
      work_queue_name: low
    job_variables:
      command: |
        pixi --manifest-path /flows/demo1/pixi.toml run python -m prefect.engine
      working_dir: /flows/demo1